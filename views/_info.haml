.info
  - if @results["matches"] > 4
    - if @last_record != @results["matches"]
      %p
        Showing #{@start+1} to #{@last_record} of 
        - if @results["matches"] > @start + @page_size
          about
        #{@results["matches"]} results
  - unless @filter.empty?
    .filter 
      - if @filter[:member]
        Contributions by
        #{h(@filter[:member])}
      - else
        #{h(@filter["section"])}
      %a.remove-filter{:href => "?q=#{params[:q].gsub("+", "%2B")}", :title => "Remove this filter"} &times;